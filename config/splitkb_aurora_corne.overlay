/ {
    chosen {
        zmk,display = &nice_view;
    };
};

/* Wir überschreiben nur den einen Bus, den das nice!view Shield sucht */
&nice_view_spi {
    status = "okay";
    cs-gpios = <&gpio0 6 GPIO_ACTIVE_HIGH>;
};

&nice_view {
    status = "okay";
};

/* Absolute Notbremse: Wir sagen dem i2c Bus, er soll die Finger von den Pins lassen */
&i2c0 { status = "disabled"; };
&i2c1 { status = "disabled"; };
